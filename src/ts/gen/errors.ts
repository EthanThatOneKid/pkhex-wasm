/**
 * Concrete error taxonomy of the v1 surface.
 *
 * @generated by tools/apigen — regenerate with `deno task gen`; do not edit.
 */
/** Thrown by {@link PKHex.load} for unrecognized or corrupt buffers. */
export class SaveParseError extends Error {
    constructor(message = "Unrecognized save file format.") {
        super(message);
        this.name = "SaveParseError";
    }
}

/** Thrown when a mutator is called on a read-only-tier generation (Gen 1–2, LGPE, PLA). The message names the unsupported operation. */
export class UnsupportedTierError extends Error {
    constructor(message = "Operation is not supported on this save's support tier.") {
        super(message);
        this.name = "UnsupportedTierError";
    }
}

/** Thrown when an operation has no meaning for the entity's generation (e.g. setting natures before Gen 3). */
export class UnsupportedOperationError extends Error {
    constructor(message = "Operation has no meaning for this entity's generation.") {
        super(message);
        this.name = "UnsupportedOperationError";
    }
}

/** Placeholder body for binding seams not yet wired to the wasm host. */
export function notImplemented(seam: string): never {
    throw new Error(`pkhex-wasm: not implemented — ${seam}`);
}
